"""StockMeta Pro — UI layer (CustomTkinter only).

Backend lives in ``src/modules/`` and is reached exclusively through the
``ShutterstockAIv2`` facade. UI never imports a backend implementation
class directly.
"""
